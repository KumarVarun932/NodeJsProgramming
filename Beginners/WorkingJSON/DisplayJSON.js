const language = require("./Data")

console.log(language);//It brings the output as an objects with everything
console.log(language.English); //This allow us to bring the content of the JSON file with Key vaue
console.log(language.Hindi);